// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;

int main() {
    // Write C++ code here
    int x = 10, y = 5;
    int add = x + y;
    int sub = x - y;
    int mul = x * y;
    int div = x / y;
    int module = x % y;
    int inc = ++x;
    int dec = --y;
    cout << "\naddition is : " << add;
    cout << "\nsubstraction is : " << sub;
    cout << "\nmultiplication is : " << mul;
    cout << "\ndivision is : " << div;
    cout << "\nmodule is : " << module;
    cout << "\nincrement is : " << inc;
    cout << "\ndecrement is : " << dec;
    
    return 0;
}